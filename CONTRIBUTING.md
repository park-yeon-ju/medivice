# 메디바이스 협업 규칙

**이 문서는 사람과 AI 에이전트가 함께 읽는 단일 규칙 파일입니다.**
팀원 전원이 에이전트를 쓰기 때문에, 다섯 사람의 에이전트가 만든 결과물이 한 사람이 쓴 것처럼 보이게 하는 것이 이 문서의 목적입니다.

> **에이전트에게 작업을 시키기 전에 이 파일을 먼저 읽히세요.**
> Claude Code는 루트의 `CLAUDE.md`를 통해 자동으로 불러옵니다. 다른 도구를 쓴다면 세션 시작 시 이 파일을 컨텍스트에 넣어주세요.

---

## 0. 프로젝트 전제

이 규칙은 아래 전제 위에서 만들어졌습니다. 전제가 바뀌면 규칙도 바꿔야 합니다.

| 항목 | 값 |
| --- | --- |
| 기간 | 3일 (발표 · 3일차 15:00) |
| 과정 범위 | 서비스 기획 & 아키텍처 → **시스템 설계 & 스캐폴딩**까지 |
| 범위 밖 | 애플리케이션 개발, 테스트·디버깅 (강의 가이드 5쪽) |
| 구조 | **단일 레포 · 단일 백엔드 · 단일 프론트** (MSA 아님) |
| 스택 | Vue 3 (Vite) / Spring Boot / PostgreSQL / Mock AI |
| 목표 | 완성된 앱이 아니라 **설계가 논리적이고 확장 가능함을 설득하는 것** |

**그래서 이 규칙은 의도적으로 가볍습니다.** 3일 안에 프로세스가 진도를 잡아먹으면 안 되므로, 리뷰 강제·이슈 트래커·릴리스 브랜치 같은 건 넣지 않았습니다. 대신 **깨지면 팀 전체가 멈추는 지점**에만 규칙을 걸었습니다.

---

## 1. 역할과 파일 소유권

우리는 **경로가 경계**입니다. 자기 경로 안은 자유롭게, 남의 경로는 요청으로.

| 경로 | 소유 | 남이 고칠 때 |
| --- | --- | --- |
| `frontend/src/**` | **FE** | FE에게 요청 |
| `backend/src/**/entity/**` | **DA** (BE와 공동) | ⚠️ 5장 참고 — 스키마가 바뀜 |
| `backend/src/**` (entity 제외) | **BE** | BE에게 요청 |
| `backend/src/**/ai/**` | **API Architect** | 스키마 변경은 공지 |
| `docs/**`, `*.html` | 각 문서 작성자 | 자유 (문서는 충돌 나도 손해가 작음) |
| `docker-compose.yml`, `.env.example`, `README.md`, `CONTRIBUTING.md` | **PM / DevOps** | 공지 후 |

> 한 사람이 여러 역할을 겸해도 됩니다. 중요한 건 **어떤 파일에 대해 누가 최종 결정권을 갖는지**가 정해져 있는 것입니다.

---

## 2. 절대 규칙 (사람·에이전트 공통)

이 목록만은 예외 없습니다. 에이전트에게도 그대로 적용됩니다.

- **API 키·비밀번호·DB 접속정보를 커밋하지 않는다.** `.env`에 두고 `.env.example`만 올린다.
- **강의 자료(PDF·녹취록)를 커밋하지 않는다.** SK㈜ AX 저작물로 재배포가 금지되어 있다.
- **의존성을 임의로 추가하지 않는다.** 3일 안에 빌드가 깨지면 복구할 시간이 없다. 새 라이브러리가 필요하면 담당자에게 먼저 말한다.
- **자기 소유가 아닌 경로의 파일을 고치지 않는다.** 필요하면 소유자에게 요청한다.
- **`main`에 직접 push하지 않는다.** 브랜치 → PR.
- **모든 코드 파일에 주석을 단다.** 파일 최상단에는 이 파일이 무엇인지 요약을, 코드 안에는 왜 그렇게 짰는지를 한국어로 적는다. 주석 없는 코드는 머지하지 않는다. (→ 4.7)
- **AI가 만든 코드는 본인이 설명할 수 있어야 한다.** 교수가 중간에 확인하며 개별 점수에 반영합니다. 이해 못 한 코드는 머지하지 않는다.

---

## 3. 브랜치 · 커밋 · PR

### 3.1 브랜치

**영역별 브랜치를 미리 만들어 두고 각자 자기 브랜치에서 작업합니다.** 작업할 때마다 새로 만들지 않습니다.

```
main                 항상 데모 가능한 상태 — 3일차 발표에서 이걸 그대로 띄움
├─ frontend          FE
├─ backend           BE
├─ db                DA
├─ docs              PM · 기획
└─ infra             DevOps
```

브랜치 이름은 1장의 **경로 소유권과 짝**이 맞습니다. `frontend` 브랜치에서 일하는 사람이 `frontend/src/**`의 주인입니다.

> **⚠️ 하루에 최소 한 번은 `main`으로 머지합니다.**
> 이게 이 방식의 유일한 안전장치입니다. 영역 브랜치를 3일 내내 안 합치면 마지막 날 다섯 갈래가 한꺼번에 충돌합니다. 특히 FE는 BE가 만든 API를 봐야 하므로, **작업이 다 끝나지 않았어도 돌아가는 상태면 올리세요.**

작업 시작 전에는 `main`의 최신 내용을 자기 브랜치로 가져옵니다.

```bash
git checkout frontend
git merge main
```

**한 영역을 두 명 이상이 맡는 경우에만** 그 아래에 작업 브랜치를 팝니다.

```
frontend/feat/medilight-banner
frontend/fix/dark-mode-contrast
```

`main`에 직접 커밋하지 않습니다.

### 3.2 커밋

```
type(scope): 설명
```

- **scope**: `frontend` `backend` `db` `docs` `infra` — 필수
- 설명은 한국어, **명령형으로 간결하게** ("추가함" ❌ → "추가" ✅)

```
feat(backend): 성분별 하루 총량 합산 API 추가
feat(frontend): 메디라이트 배너 컴포넌트 추가
fix(frontend): 다크모드에서 상태 칩 대비 부족 수정
chore(db): 제품·성분 시드 데이터 10건 추가
docs(docs): API 명세에 미등록 성분 응답 필드 반영
```

**여러 영역에 걸친 커밋은 쪼갭니다.** 프론트와 백엔드를 한 커밋에 담지 않습니다 — 롤백 단위를 영역 단위로 유지하기 위함입니다.

### 3.3 PR

영역 브랜치 → `main`으로 PR을 올립니다. 3일짜리라 리뷰를 강제하지 않고, **영향 범위에 따라** 나눕니다.

| 변경 | 절차 |
| --- | --- |
| 자기 소유 경로 안에서 끝남 | **셀프 머지 OK** |
| **API 계약** 변경 (경로·요청·응답 필드) | PR에 `[계약 변경]` 표시 + FE·BE 양쪽 확인 |
| **엔티티/스키마** 변경 | PR에 `[DB 변경]` 표시 + DA·BE 합의 (5장) |
| `docker-compose.yml`, 포트, 환경변수 | 팀 전체 공지 |

PR 제목은 커밋과 같은 형식을 씁니다.

**머지한 뒤 브랜치를 지우지 마세요.** 영역 브랜치는 계속 씁니다. GitHub이 머지 후 `Delete branch` 버튼을 권하는데, 누르지 않습니다.

---

## 4. 코드 규칙

### 4.1 백엔드 패키지 구조 (고정)

```
com.medivice
 ├─ config/       GlobalExceptionHandler, JpaConfig, WebConfig
 ├─ controller/
 ├─ dto/          도메인별 {Domain}Dto.java, 중첩 static class로 Request/Response
 ├─ entity/
 ├─ repository/
 ├─ service/
 │   └─ rule/     규칙 엔진 (성분 합산·중복·임계값 판정)
 └─ ai/           AiClient 인터페이스 + MockAiClient / (향후) OpenAiClient
```

두 패키지가 이 프로젝트의 핵심입니다.

- **`service/rule/`** — 결정론적 판정. 같은 입력에 항상 같은 결과가 나와야 하고, **AI를 호출하지 않습니다.**
- **`ai/`** — 확률적 영역. 반드시 인터페이스 뒤에 둡니다.

```java
public interface AiClient {
    OcrResult extract(String imageId);          // EXT-1
    String explain(FindingContext context);      // EXT-2
    ReportResult report(ReportRequest request);  // EXT-3
}
```

구현체는 `ai.provider` 설정값으로 갈아끼웁니다. **컨트롤러나 서비스가 구현체를 직접 참조하지 않습니다.**

```yaml
# application.yml
ai:
  provider: mock        # mock | openai
```

### 4.2 API 응답 포맷

외부(프론트 호출) 엔드포인트는 `ApiResponse<T>`로 감쌉니다.

```java
ApiResponse.success(data)      // 성공
ApiResponse.error(message)     // 실패
```

```json
{ "success": true, "data": { ... }, "message": null }
```

**엔드포인트마다 포맷을 다르게 만들지 마세요.** 프론트가 응답마다 파싱을 따로 짜야 하는 상황을 만듭니다.

### 4.3 HTTP 상태 코드 (고정)

> 루브릭이 **"RESTful 규격(Method, Path, Status Code) 준수 여부"**를 직접 채점합니다. 아래를 그대로 따르세요.

| 상황 | 코드 |
| --- | --- |
| 조회 성공 | `200 OK` |
| 생성 성공 | `201 Created` |
| 삭제 성공 | `204 No Content` |
| **AI 비동기 작업 접수** | `202 Accepted` (+ `job_id` 반환) |
| 잘못된 요청 · 검증 실패 | `400 Bad Request` |
| 리소스 없음 | `404 Not Found` |
| 서버 오류 | `500 Internal Server Error` |

비동기 작업 상태는 네 가지로 고정합니다.

```
PENDING → PROCESSING → COMPLETED | FAILED
```

### 4.4 예외 처리

`GlobalExceptionHandler`(`@RestControllerAdvice`)에 3종만 둡니다.

| 예외 | 상태 | 응답 |
| --- | --- | --- |
| `IllegalArgumentException` | 400 | `ApiResponse.error(e.getMessage())` |
| `MethodArgumentNotValidException` | 400 | 필드 에러 메시지 조합 |
| `Exception` (그 외 전부) | 500 | **고정 문구** `"서버 오류가 발생했습니다"` |

**스택트레이스나 원본 예외 메시지를 응답에 그대로 노출하지 마세요.** 데모 화면에 내부 구조가 찍힙니다.

### 4.5 컨트롤러

```java
@RestController
@RequestMapping("/api/medications")     // 복수형 리소스
@RequiredArgsConstructor
public class MedicationController {

    private final MedicationService medicationService;

    /** POST /api/medications - 복용 항목 등록 (인증 필요) */
    @PostMapping
    public ResponseEntity<ApiResponse<MedicationDto.Response>> create(
            @RequestHeader("X-User-Id") Long userId,
            @Valid @RequestBody MedicationDto.CreateRequest request) { ... }
}
```

- 경로는 **복수형 명사**: `/api/medications` `/api/symptoms` `/api/reports`
- 각 메서드 위에 `METHOD /path - 설명 (인증 필요/불필요)` javadoc
- 사용자 식별은 `@RequestHeader("X-User-Id") Long userId`로만 받습니다. 컨트롤러에서 JWT를 직접 파싱하지 않습니다.
  - **이번 3일 범위에서는 인증을 구현하지 않습니다.** 고정 사용자 ID를 헤더로 주입해 데모합니다. 헤더 방식을 지금 정해두면 나중에 인증을 붙일 때 컨트롤러를 안 고쳐도 됩니다.

### 4.6 네이밍

| 대상 | 규칙 | 예 |
| --- | --- | --- |
| DTO | `XxxRequest` / `XxxResponse` | `MedicationCreateRequest` |
| Enum | 대문자 스네이크 | `OK` `WARN` `CRIT` / `REGULAR` `PRN` |
| DB 테이블 | snake_case 복수형 | `user_medications` |
| DB 컬럼 | snake_case | `daily_total` |
| Java 필드 | camelCase (JPA 자동 매핑에 맡김) | `dailyTotal` |
| Vue 컴포넌트 | PascalCase | `MediLightBanner.vue` |

`@Column(name = ...)`을 남발하지 마세요. 자동 매핑으로 충분합니다.

### 4.7 주석과 파일 헤더 (필수)

> **모든 코드 파일은 주석이 있어야 합니다. 예외 없습니다.**
> 팀원 전원이 AI 에이전트로 코드를 만들기 때문에, 주석이 없으면 **아무도 그 코드를 설명할 수 없는 상태**가 됩니다. 강의 평가에 `설명 가능성 — 각 담당자는 AI가 만든 결과를 검토하고 자신의 구현 개념을 설명` 항목이 있고, 교수가 중간에 확인하며 개별 점수에 반영합니다.

#### ① 파일 최상단에 요약을 답니다

**모든 파일**의 맨 위에 "이 파일이 무엇인지"를 적습니다. 파일을 처음 여는 사람이 이 부분만 읽고도 역할을 알 수 있어야 합니다.

담을 내용은 넷입니다 — **무엇을 하는 파일인지 / 어떻게 동작하는지 / 주의할 점 / 관련 UC·화면 ID**

```java
/**
 * 성분별 하루 총량을 합산하고 중복·상한 초과를 판정하는 규칙 엔진.
 *
 * 사용자가 복용 중인 모든 제품의 성분을 정규화된 단위로 더한 뒤,
 * 같은 성분이 2개 이상의 제품에 있으면 중복(WARN),
 * 설정된 상한을 넘으면 초과(CRIT)로 판정한다.
 *
 * AI를 호출하지 않는다. 같은 입력에는 항상 같은 결과가 나와야 하므로
 * 여기서는 규칙과 DB 조회만 사용한다.
 *
 * 관련 UC: UC15(성분 충돌·중복 판정), UC16(메디라이트 표시), UC31(미등록 성분 안내)
 */
public class MediLightRuleEngine { ... }
```

```vue
<!--
  MediLightBanner.vue
  메인 화면 상단에 메디라이트 상태를 보여주는 배너.

  규칙 엔진이 판정한 상태(OK / WARN / CRIT)를 받아 신호등 표시 + 색 + 텍스트
  세 가지로 함께 보여준다. 색만 쓰면 색각 이상 사용자에게 정보가 사라지므로
  세 가지를 모두 유지해야 한다.

  확인하지 못한 성분이 있으면 "성분 11개 중 8개 확인"처럼 커버리지를 함께 표시한다.

  관련 UC: UC16(메디라이트 표시), UC31(미등록 성분 안내) / 화면: SCR-MAIN-001
-->
```

#### ② 코드 안에는 "왜"를 씁니다

문법 설명이 아니라 **도메인 맥락과 판단 근거**를 적습니다. 코드를 처음 보는 사람이 이해할 수 있어야 합니다.

| ❌ 이런 주석은 쓰지 마세요 | ✅ 이렇게 쓰세요 |
| --- | --- |
| `// i를 1 증가시킨다` | `// 제품 하나에 성분이 여러 개라 성분 단위로 다시 순회한다` |
| `// 리스트를 반환한다` | `// 마스터에 없는 성분은 합산에서 빼고 별도 목록으로 돌려준다 (UC31)` |
| `// null 체크` | `// AI 설명은 실패할 수 있으므로 없어도 판정 결과는 그대로 보여준다` |

주석이 꼭 필요한 자리는 다음과 같습니다.

- **도메인 지식이 있어야 이해되는 계산** — 단위 변환, 임계값, 판정 기준
- **일부러 그렇게 짠 부분** — 왜 이 방식을 골랐는지
- **안 그러면 오해할 코드** — 비어 보이는 분기, 예외를 삼키는 곳

```java
// 비타민D는 제품마다 IU와 μg를 섞어 쓴다. 그냥 더하면 값이 틀리므로
// 저장 시점에 정한 canonical_unit(IU)으로 변환한 뒤 합산한다. (1 μg = 40 IU)
double amountInCanonicalUnit = ingredient.toCanonicalUnit(product.getAmount());

// 처방약은 의사가 정한 용량이므로 상한 비교 대상에서 뺀다.
// 여기서 CRIT을 띄우면 "처방약 최우선" 원칙과 충돌한다.
if (item.isPrescription()) continue;
```

#### ③ 규칙

- 주석은 **한국어**로 씁니다. 팀원과 평가자가 읽습니다.
- 코드를 고치면 **주석도 같이 고칩니다.** 틀린 주석은 없는 것보다 나쁩니다.
- 에이전트에게 시킬 때 **"주석 필수"를 프롬프트에 넣으세요.** 안 넣으면 대부분 안 답니다.

  > `모든 파일 최상단에 이 파일이 뭘 하는지 요약 주석을 달고,`
  > `코드 안에는 왜 그렇게 짰는지 한국어 주석을 달아줘.`

- 주석이 없는 코드는 **머지하지 않습니다.** PR에서 발견하면 돌려보냅니다.

---

## 5. 데이터베이스

### 5.1 ddl-auto 주의

```yaml
spring.jpa.hibernate.ddl-auto: update
```

3일 범위에서는 마이그레이션 도구 없이 이 설정으로 갑니다. 대신 **엔티티를 고치는 순간 다음 기동 때 스키마가 바뀝니다.**

- 엔티티 변경 PR에는 **`[DB 변경]`**을 반드시 표시
- 컬럼 삭제·타입 변경은 **DA와 BE 합의 후**
- 시드 데이터는 `backend/src/main/resources/data.sql` 한 곳에서만 관리

### 5.2 도메인 규칙

이 프로젝트에서 **반드시 지켜야 할 데이터 설계**입니다. 기획서에서 나온 것이라 임의로 바꾸지 마세요.

- **성분은 정규화된 단위로 저장한다.** `ingredient.canonical_unit`을 기준으로 **저장 시점에 변환**합니다. 조회할 때 변환하면 화면마다 반올림이 달라집니다.
  - 예: 비타민D `1 μg = 40 IU`
- **`ingredient_id`는 null을 허용한다.** 성분 마스터에 없는 항목도 등록은 성공해야 합니다. 사용자가 입력한 원본은 `raw_name`에 보존합니다. (UC31)
- **삭제는 소프트 삭제**(`deleted_at`)로 한다. 하드 삭제하면 과거 증상 기록의 복용 스냅샷이 깨집니다.
- **증상 스냅샷은 참조가 아니라 값 복사**로 저장한다. 그날의 사실을 그대로 보존해야 합니다.

---

## 6. 프론트엔드 규칙

### 6.1 구조

```
frontend/src/
 ├─ assets/tokens.css      디자인 토큰 (단일 진실 공급원)
 ├─ layouts/AppShell.vue   레일 + 상단바 + <RouterView/>
 ├─ components/            재사용 컴포넌트
 ├─ views/                 화면 = 라우트 단위
 └─ api/client.js          Mock ↔ 실서버 전환 지점
```

- Vue 3 **Composition API + `<script setup>`** 사용
- 컴포넌트 파일명 PascalCase, 화면은 `XxxView.vue`

### 6.2 색은 반드시 토큰으로

> **에이전트가 가장 자주 어기는 규칙입니다.** 색 하드코딩을 발견하면 되돌리세요.

```css
/* ✅ */  color: var(--emerald);
/* ❌ */  color: #00996F;
/* ❌ */  color: rgb(0, 153, 111);
```

`tokens.css`에 없는 색이 필요하다고 판단되면, 색을 만들지 말고 **FE에게 물어보세요.** 대부분 이미 있는 토큰으로 해결됩니다.

### 6.3 상태색은 상태에만

`--warn`(노랑)과 `--crit`(빨강)은 **메디라이트·경고 칩·낮은 신뢰도 배지에만** 씁니다.

버튼·링크·장식에 쓰지 마세요. 삭제 버튼도 빨강으로 만들지 않습니다. 화면 곳곳에 노랑이 흩어지면 진짜 "주의"가 눈에 들어오지 않습니다.

### 6.4 다크모드

모든 색은 **기본 `:root`에 먼저 정의**하고, 다크 블록에서 값만 덮어씁니다.

```css
:root { --surface: #FFFFFF; }                         /* ✅ 항상 여기 먼저 */
@media (prefers-color-scheme: dark) {
  :root:not([data-theme="light"]) { --surface: #0E2320; }
}
:root[data-theme="dark"] { --surface: #0E2320; }
```

`@media`나 `[data-theme]` **안에서만** 정의된 색은 OS 설정이 "시스템"인 사용자에게 적용되지 않습니다.

---

## 7. 의료 안전 문구 규칙 ⚠️

**이 프로젝트에만 있는 규칙이고, 가장 중요합니다.**
에이전트는 자연스러운 카피를 만들려다 이 선을 자주 넘습니다. UI 문구·에러 메시지·AI 프롬프트 전부에 적용됩니다.

### 쓰지 말 것

| ❌ 금지 | 이유 |
| --- | --- |
| "안전합니다" / "문제없습니다" | 우리는 안전을 보장하지 않습니다 |
| "위험합니다" / "복용을 중단하세요" | 진단·처방 변경을 지시하지 않습니다 |
| "이 약 때문에 증상이 생겼습니다" | 인과관계를 판정하지 않습니다 |

### 쓸 것

| 상황 | 문구 |
| --- | --- |
| 초록 상태 | **"확인된 문제 없음"** — 현재 적재된 규칙에서 문제가 발견되지 않았다는 뜻 |
| 미등록 성분 | **"○○ 성분은 현재 데이터베이스에 없어 중복과 하루 총량을 확인하지 못했습니다. 의사나 약사에게 확인해 보세요."** |
| 불확실할 때 | 확인하지 못한 **사실만** 말하고, 판단은 의료전문가에게 넘긴다 |

### 표시 규칙

- **메디라이트는 색·형태·글자 3중으로** 표시합니다. 신호등 표시 + 색 + "노랑 · 주의" 텍스트. 색만 쓰면 색각 이상 사용자에게 정보가 사라집니다.
- **AI가 만든 문장은 규칙 엔진 결과와 시각적으로 분리**합니다. 점선 테두리 + `AI 설명` 라벨 + 출처·확인일. 사용자가 둘을 같은 신뢰도로 받아들이면 안 됩니다.
- **AI 응답이 없어도 화면이 동작해야 합니다.** `ai_explanation`이 `null`이거나 `FAILED`여도 판정 결과(색·수치·근거)는 그대로 보여야 합니다.

---

## 8. 로컬 실행

| 서비스 | 포트 |
| --- | --- |
| Frontend (Vite) | `5173` |
| Backend (Spring Boot) | `8080` |
| PostgreSQL | `5432` |
| Swagger UI | `8080/swagger-ui/index.html` |

PR 올리기 전 확인:

- [ ] 백엔드 — `./gradlew build` 통과
- [ ] 프론트 — `npm run build` 통과
- [ ] 화면이 실제로 뜨는지 브라우저에서 확인
- [ ] API를 바꿨다면 **프론트도 같이 띄워서** 확인

---

## 9. AI 에이전트 사용 규칙

전원이 에이전트를 쓰기 때문에 이 절이 필요합니다.

### 9.1 시작할 때

1. 세션 시작 시 **이 파일을 컨텍스트에 넣습니다.**
2. 작업 범위를 **자기 소유 경로로 한정**해서 지시합니다.
   > "backend/src/main/java/com/medivice/service/rule/ 안에서만 작업해줘"
3. 관련 설계 문서를 같이 읽힙니다. (`메디바이스_기획서.html`, `스프린트 계획.html`)

### 9.2 에이전트에게 시키지 말 것

- **여러 영역을 한 번에** 고치게 하지 마세요. 프론트와 백엔드를 동시에 시키면 커밋이 섞이고 충돌이 납니다.
- **의존성 추가**를 맡기지 마세요. 빌드가 깨지면 3일 안에 복구할 시간이 없습니다.
- **엔티티 변경**을 무단으로 시키지 마세요. `ddl-auto: update`라 DB가 바로 바뀝니다.
- **디자인 토큰 밖의 색**을 만들게 두지 마세요.

### 9.3 머지 전 사람이 확인할 것

- [ ] 내가 이 코드를 **설명할 수 있는가** ← 교수가 확인하며 개별 점수에 반영합니다
- [ ] 자기 소유 경로 밖의 파일을 건드리지 않았는가
- [ ] 하드코딩된 색·API 키·비밀번호가 없는가
- [ ] 7장의 의료 안전 문구 규칙을 지켰는가
- [ ] 응답 포맷과 상태 코드가 4장과 일치하는가

### 9.4 커밋은 사람이

에이전트가 작성한 diff를 **읽고 나서** 커밋하세요. 자동 커밋은 쓰지 않습니다.

---

## 10. 문서

- **Swagger가 API 문서입니다.** API를 바꾸면 springdoc 어노테이션(요약·설명)을 같이 갱신하세요.
- 설계 문서(`*.html`)는 정적 파일이라 자동 동기화되지 않습니다. **API 명세·ERD·화면이 바뀌면 해당 문서도 담당자가 갱신**합니다.
- Use-Case 번호(UC1~UC31)와 화면 ID(SCR-XXX-000)는 **기획서가 기준**입니다. 새로 만들지 말고 기획서에서 가져다 쓰세요.

---

## 부록 · 이 문서를 고칠 때

규칙이 실제 작업을 방해하면 고치는 게 맞습니다. 다만 **PM에게 말하고, 팀 전체에 공지한 뒤** 고치세요. 절반은 옛 규칙으로, 절반은 새 규칙으로 만든 코드가 섞이는 게 규칙이 없는 것보다 나쁩니다.
