# 메디바이스 — 에이전트 지침

**작업을 시작하기 전에 [`CONTRIBUTING.md`](CONTRIBUTING.md)를 읽으세요.** 팀의 코드 컨벤션·협업 규칙·의료 안전 문구 규칙이 모두 그 파일에 있습니다.

아래는 그중 어겼을 때 손해가 가장 큰 항목만 옮긴 것입니다. 전체 규칙을 대신하지 않습니다.

## 프로젝트

성분 중심 복약 안전 도우미. Vue 3 (Vite) + Spring Boot + PostgreSQL + Mock AI.
**단일 레포 · 단일 백엔드 · 단일 프론트** (MSA 아님). 3일 미니 프로젝트이며 목표는 완성된 앱이 아니라 **설계와 스캐폴딩**입니다.

## 절대 하지 말 것

- **자기 소유가 아닌 경로의 파일 수정** — 소유권은 `CONTRIBUTING.md` 1장 참고
- **API 키·비밀번호·DB 접속정보 커밋** — `.env`에 두고 `.env.example`만 올릴 것
- **의존성 임의 추가** — 빌드가 깨지면 복구할 시간이 없음
- **엔티티 무단 변경** — `ddl-auto: update`라 DB 스키마가 바로 바뀜
- **디자인 토큰 밖의 색 사용** — `#00996F` ❌ / `var(--emerald)` ✅
- **강의 자료(PDF·녹취록) 커밋** — SK㈜ AX 저작물, 재배포 금지
- **`main`에 직접 push**

## 의료 안전 문구 (가장 자주 어기는 규칙)

이 서비스는 진단하지 않고, 인과관계를 판정하지 않으며, 복용 중단을 지시하지 않습니다.

- ❌ "안전합니다" · "문제없습니다" · "위험합니다" · "복용을 중단하세요"
- ✅ 초록 상태는 **"확인된 문제 없음"**
- ✅ 확인하지 못했을 때는 **그 사실만** 말하고 의사·약사 확인으로 연결
- ✅ AI가 만든 문장은 규칙 엔진 결과와 시각적으로 분리 (점선 + `AI 설명` 라벨 + 출처)
- ✅ 메디라이트는 색만이 아니라 **색 + 신호등 표시 + 텍스트 라벨** 3중으로

## 코드 기본형

- 백엔드 패키지: `com.medivice` — `config controller dto entity repository service ai`
- `service/rule/`은 **결정론적**이며 AI를 호출하지 않음. AI는 전부 `ai/`의 `AiClient` 인터페이스 뒤에
- 외부 응답은 `ApiResponse<T>`로 통일 (`success` / `error`)
- 상태 코드: 조회 `200` · 생성 `201` · 삭제 `204` · **AI 비동기 접수 `202`** · 검증 실패 `400` · 없음 `404` · 서버 오류 `500`
- 비동기 상태: `PENDING → PROCESSING → COMPLETED | FAILED`
- 사용자 식별은 `@RequestHeader("X-User-Id") Long userId`로만 (컨트롤러에서 JWT 파싱 금지)
- 프론트는 Vue 3 Composition API + `<script setup>`, 컴포넌트 PascalCase

## 커밋

```
type(scope): 설명       # scope = frontend | backend | db | docs | infra
feat(backend): 성분별 하루 총량 합산 API 추가
```

여러 영역에 걸친 변경은 커밋을 쪼갤 것.

브랜치는 **영역별로 미리 만들어져 있습니다** — `frontend` `backend` `db` `docs` `infra`. 새로 만들지 말고 자기 영역 브랜치에서 작업하세요. `main`에는 직접 커밋하지 않습니다.

## 작업 후

diff를 사람이 읽고 커밋합니다. 자동 커밋하지 마세요.
**작성자가 설명할 수 없는 코드는 머지하지 않습니다** — 교수가 중간에 확인하며 개별 점수에 반영합니다.
