# 이 파일을 .env.development.local로 복사한 뒤, 백엔드 없이 UI를 검증할 때만 true로 바꿉니다.
# adapter.js도 DEV 여부를 검사하므로 production build에서는 항상 실제 API를 사용합니다.
VITE_USE_MOCK_API=false

# 실제 API 모드에서 사용할 Spring Boot 서버 주소입니다.
VITE_API_BASE_URL=http://localhost:8080
